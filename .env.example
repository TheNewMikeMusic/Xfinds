# ===========================================
# Xfinds Environment Variables
# ===========================================
# Copy this file to .env.local and fill in the values
# cp .env.example .env.local

# ===========================================
# Required Variables
# ===========================================

# JWT Secret for authentication (minimum 32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-min-32-chars

# ===========================================
# Optional Variables
# ===========================================

# Environment mode (development | production)
NODE_ENV=development

# Authentication mode (stub | full)
# stub: Quick authentication for development
# full: Full authentication with email verification
AUTH_MODE=stub

# Application URL (used for email links, etc.)
APP_URL=http://localhost:3000

# Public application URL (accessible from client-side)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Exchange rate API endpoint
# Default uses free API, you can replace with your own
EXCHANGE_RATE_API=https://api.exchangerate-api.com/v4/latest/CNY

# ===========================================
# Production Variables (set these in production)
# ===========================================

# Admin token for admin API endpoints (optional)
# ADMIN_TOKEN=your-secure-admin-token

# ===========================================
# Notes
# ===========================================
# 
# For production deployment:
# 1. Set NODE_ENV=production
# 2. Generate a strong JWT_SECRET (openssl rand -base64 32)
# 3. Update APP_URL and NEXT_PUBLIC_APP_URL to your domain
# 4. Set ADMIN_TOKEN if using admin features
#
# Never commit .env.local to version control!
